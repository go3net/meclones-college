{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefer-offline --no-audit --no-fund && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma db push --skip-generate --accept-data-loss && npm run start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
